set up a new project, based on the document in the working dir, build a playfull, interactive calculator that can use all different formulas and variables, show the results of the calculations schematically and animated (plane rotates to TH, TC is plotted on a map, wind direction and strenght, etc, if relevant to the kind of problem being solved. The app should run with python in termux as a webserver but also in a container run from a fullscreen webbrowser, so the app should be adaptive or have a mobile and browser version. Use the proect manager skill to set up the folder structure for this project first